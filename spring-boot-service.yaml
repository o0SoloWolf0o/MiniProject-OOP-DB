apiVersion: v1
kind: Service
metadata:
  name: springboot-container-service
spec:
  type: LoadBalancer
  selector:
    app: springboot-container
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
